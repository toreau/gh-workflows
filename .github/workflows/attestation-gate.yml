# attestation-gate
#
# Consumer-side attestation gate (promotion side of the supply chain).
#   - Legacy mode (image tom): REST-eksistens + predicate-type-sjekk.
#   - Strong mode (image satt): kryptografisk `gh attestation verify` mot
#     OCI-bundlen, med forventet signer-workflow/source-ref og valgfri
#     --source-digest (kun når en uavhengig forventet source-SHA finnes).
# Brukes av: k8s-research `app-digest-bump.yml` (pre-PR, med --source-digest)
#   og `validate.yml` `gate-pr` (uten --source-digest — trusted-main provenance).
# Merknad: --signer-repo og --signer-workflow er gjensidig utelukkende; bruk
#   --signer-workflow med full owner/repo/path når begge ellers ville gjelde.
name: attestation-gate
on:
  workflow_call:
    inputs:
      owner-repo:
        required: true
        type: string
        description: Owner/repo whose attestations to query, e.g. toreau/astronomy.aursand.no
      digest:
        required: true
        type: string
        description: Full image digest, e.g. sha256:<hex>
      predicate-type:
        required: false
        type: string
        default: "https://slsa.dev/provenance/v1"
        description: Expected attestation predicate type
      # Strong mode (consumer-side provenance verification). When `image` is set,
      # success comes ONLY from `gh attestation verify` against the OCI bundle.
      # Legacy mode (image empty) keeps the REST existence/predicate check.
      image:
        required: false
        type: string
        description: "Full image reference without digest, e.g. ghcr.io/toreau/frosta-historielag.no. Set to enable strong mode."
      signer-repo:
        required: false
        type: string
        description: "Expected signer repository (owner/repo), e.g. toreau/frosta-historielag.no. Mutually exclusive with signer-workflow."
      signer-workflow:
        required: false
        type: string
        description: "Expected signer workflow with FULL owner/repo path (no @ref), e.g. toreau/frosta-historielag.no/.github/workflows/ci.yml. Mutually exclusive with signer-repo."
      source-ref:
        required: false
        type: string
        description: "Expected source ref, e.g. refs/heads/main"
      source-digest:
        required: false
        type: string
        description: "Expected source repository commit SHA (Git SHA, NOT artifact digest). Only pass when an independently trusted expected source SHA exists (e.g. HMAC-verified dispatch sha)."
    secrets:
      token:
        required: true
        description: GitHub token for attestation lookup (strong mode) or REST API (legacy mode)
      # Registry credential for strong mode (OCI image + OCI attestation bundle).
      # PAT classic with read:packages. Legacy mode uses `token` against the GitHub API.
      registry-token:
        required: false
        description: GHCR credential for strong mode (PAT classic with read:packages)
permissions:
  contents: read
jobs:
  gate:
    runs-on: ubuntu-latest
    steps:
      - name: Verify attestation (strong mode — OCI bundle, cryptographic)
        if: inputs.image != ''
        env:
          GH_TOKEN: ${{ secrets.token }}
          REG_TOKEN: ${{ secrets.registry-token }}
          IMAGE: ${{ inputs.image }}
          DIGEST: ${{ inputs.digest }}
          OWNER_REPO: ${{ inputs.owner-repo }}
          SIGNER_REPO: ${{ inputs.signer-repo }}
          SIGNER_WORKFLOW: ${{ inputs.signer-workflow }}
          SOURCE_REF: ${{ inputs.source-ref }}
          SOURCE_DIGEST: ${{ inputs.source-digest }}
          PRED: ${{ inputs.predicate-type }}
        run: |
          if [ -n "$REG_TOKEN" ]; then
            echo "$REG_TOKEN" | docker login ghcr.io -u "$GITHUB_ACTOR" --password-stdin >/dev/null 2>&1
          fi
          args=(verify "oci://${IMAGE}@${DIGEST}" --repo "$OWNER_REPO" --bundle-from-oci
                --predicate-type "$PRED")
          # --signer-repo and --signer-workflow are in the same cert-identity
          # flag group and are MUTUALLY EXCLUSIVE. Prefer --signer-workflow with
          # full owner/repo path when both are provided (it carries repo identity).
          [ -n "$SIGNER_WORKFLOW" ] && { args+=(--signer-workflow "$SIGNER_WORKFLOW"); }
          [ -z "$SIGNER_WORKFLOW" ] && [ -n "$SIGNER_REPO" ] && args+=(--signer-repo "$SIGNER_REPO")
          [ -z "$SOURCE_REF" ]     || args+=(--source-ref "$SOURCE_REF")
          [ -z "$SOURCE_DIGEST" ]  || args+=(--source-digest "$SOURCE_DIGEST")
          gh attestation "${args[@]}"
          echo "strong verification OK: ${IMAGE}@${DIGEST} signed by ${SIGNER_REPO}/${SIGNER_WORKFLOW} (${SOURCE_REF})"
      - name: Verify attestation exists (legacy mode — REST existence/predicate)
        if: inputs.image == ''
        env:
          GH_TOKEN: ${{ secrets.token }}
          REPO: ${{ inputs.owner-repo }}
          DIGEST: ${{ inputs.digest }}
          PRED: ${{ inputs.predicate-type }}
        run: |
          api="repos/${REPO}/attestations/${DIGEST}"
          resp=$(gh api "$api" 2>/dev/null) || { echo "no attestations for ${DIGEST} (404)"; exit 1; }
          n=$(printf '%s' "$resp" | jq '.attestations | length')
          [ "$n" -ge 1 ] || { echo "no attestations for ${DIGEST}"; exit 1; }
          preds=$(printf '%s' "$resp" | jq -r '[.attestations[].bundle.dsseEnvelope.payload | @base64d | fromjson | .predicateType] | join(" ")')
          echo "verified ${n} attestation(s) for ${DIGEST}: ${preds}"
          case " ${preds} " in
            *" ${PRED} "*) echo "predicate OK (${PRED})" ;;
            *) echo "predicate ${PRED} missing (${preds})"; exit 1 ;;
          esac
